# UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform the developer-tool aesthetic into a warm, approachable church production app with the "Soft & Inviting" design direction — warm colors, pill tabs, serif scripture, and always-visible broadcast Preview/Program monitors.

**Architecture:** Update CSS color tokens for the warm palette, restyle tabs as pills, improve typography sizing, restructure the right panel to include stacked Preview/Program monitors, and create a new BroadcastMonitor component. All changes are frontend-only.

**Tech Stack:** React 19, TypeScript, Tailwind v4, OKLCH colors, existing component patterns

---

## File Map

### New Files

| File | Responsibility |
|------|---------------|
| `src/components/broadcast/broadcast-monitor.tsx` | Stacked Preview + Program monitors with TAKE/Off Air controls |

### Modified Files

| File | Changes |
|------|---------|
| `src/index.css` | Replace color tokens with warm palette (light + dark) |
| `src/components/layout/panel-tabs.tsx` | Pill-shaped tabs replacing underline tabs |
| `src/components/ui/panel-header.tsx` | Bigger text, remove dark-only shadow |
| `src/components/layout/workspace.tsx` | Restructure right panel (queue + broadcast monitor), remove Broadcast Preview from center tabs |
| `src/components/layout/toolbar.tsx` | Fix badge size, warm "Manna" label |
| `src/components/layout/menu-bar.tsx` | Warm background color, primary-colored "Manna" |
| `src/components/panels/detections-panel.tsx` | Pill buttons, warm card styling, serif verse text |
| `src/components/panels/queue-panel.tsx` | Active item green styling, rounded cards |

---

## Task 1: Warm Color Tokens

**Files:**
- Modify: `src/index.css`

- [ ] **Step 1: Replace light mode tokens**

In `src/index.css`, replace the `:root` block (lines 76-120) with:

```css
:root {
    --background: oklch(0.965 0.008 75);
    --foreground: oklch(0.2 0.01 50);
    --card: oklch(0.98 0.005 75);
    --card-foreground: oklch(0.2 0.01 50);
    --popover: oklch(0.99 0.003 75);
    --popover-foreground: oklch(0.2 0.01 50);
    --primary: oklch(0.45 0.1 155);
    --primary-foreground: oklch(0.98 0.01 155);
    --secondary: oklch(0.94 0.008 75);
    --secondary-foreground: oklch(0.3 0.01 50);
    --muted: oklch(0.94 0.008 75);
    --muted-foreground: oklch(0.55 0.02 60);
    --accent: oklch(0.94 0.008 75);
    --accent-foreground: oklch(0.3 0.01 50);
    --destructive: oklch(0.577 0.245 27.325);
    --border: oklch(0.91 0.01 70);
    --input: oklch(0.91 0.01 70);
    --ring: oklch(0.45 0.1 155);
    --chart-1: oklch(0.65 0.15 155);
    --chart-2: oklch(0.55 0.12 155);
    --chart-3: oklch(0.75 0.1 155);
    --chart-4: oklch(0.45 0.1 155);
    --chart-5: oklch(0.35 0.08 155);
    --radius: 0.625rem;
    --sidebar: oklch(0.97 0.005 75);
    --sidebar-foreground: oklch(0.2 0.01 50);
    --sidebar-primary: oklch(0.45 0.1 155);
    --sidebar-primary-foreground: oklch(0.98 0.01 155);
    --sidebar-accent: oklch(0.94 0.008 75);
    --sidebar-accent-foreground: oklch(0.3 0.01 50);
    --sidebar-border: oklch(0.91 0.01 70);
    --sidebar-ring: oklch(0.45 0.1 155);
    --ai-direct: oklch(0.65 0.2 155);
    --ai-semantic: oklch(0.55 0.2 280);
    --ai-cloud: oklch(0.6 0.2 300);
    --confidence-high: oklch(0.65 0.2 155);
    --confidence-mid: oklch(0.7 0.15 80);
    --confidence-low: oklch(0.6 0.2 25);
    --live-pulse: oklch(0.6 0.25 25);
    --surface-elevated: oklch(1 0 0);
    --surface-sunken: oklch(0.93 0.008 75);
    --menu-bar-height: 28px;
    --toolbar-height: 40px;
}
```

- [ ] **Step 2: Replace dark mode tokens**

Replace the `.dark` block (lines 122-163) with:

```css
.dark {
    --background: oklch(0.16 0.008 50);
    --foreground: oklch(0.93 0.005 75);
    --card: oklch(0.19 0.008 50);
    --card-foreground: oklch(0.93 0.005 75);
    --popover: oklch(0.19 0.008 50);
    --popover-foreground: oklch(0.93 0.005 75);
    --primary: oklch(0.45 0.1 155);
    --primary-foreground: oklch(0.98 0.01 155);
    --secondary: oklch(0.24 0.008 50);
    --secondary-foreground: oklch(0.93 0.005 75);
    --muted: oklch(0.22 0.006 50);
    --muted-foreground: oklch(0.6 0.01 60);
    --accent: oklch(0.24 0.008 50);
    --accent-foreground: oklch(0.93 0.005 75);
    --destructive: oklch(0.65 0.2 25);
    --border: oklch(1 0 0 / 8%);
    --input: oklch(1 0 0 / 12%);
    --ring: oklch(0.55 0.12 155);
    --chart-1: oklch(0.65 0.15 155);
    --chart-2: oklch(0.55 0.12 155);
    --chart-3: oklch(0.75 0.1 155);
    --chart-4: oklch(0.45 0.1 155);
    --chart-5: oklch(0.35 0.08 155);
    --sidebar: oklch(0.19 0.008 50);
    --sidebar-foreground: oklch(0.93 0.005 75);
    --sidebar-primary: oklch(0.6 0.15 155);
    --sidebar-primary-foreground: oklch(0.2 0.05 155);
    --sidebar-accent: oklch(0.24 0.008 50);
    --sidebar-accent-foreground: oklch(0.93 0.005 75);
    --sidebar-border: oklch(1 0 0 / 8%);
    --sidebar-ring: oklch(0.55 0.12 155);
    --ai-direct: oklch(0.7 0.18 155);
    --ai-semantic: oklch(0.65 0.18 280);
    --ai-cloud: oklch(0.65 0.18 300);
    --confidence-high: oklch(0.7 0.18 155);
    --confidence-mid: oklch(0.7 0.15 80);
    --confidence-low: oklch(0.65 0.2 25);
    --live-pulse: oklch(0.65 0.25 25);
    --surface-elevated: oklch(0.22 0.006 50);
    --surface-sunken: oklch(0.14 0.006 50);
}
```

- [ ] **Step 3: Verify**

```bash
cd /Users/uxderrick-mac/Development/Manna
export PATH="$HOME/.bun/bin:$PATH"
bun run typecheck
```

- [ ] **Step 4: Commit**

```bash
git add src/index.css
git commit -m "feat: warm color palette — cream/green light mode, warm dark mode"
```

---

## Task 2: Pill Tabs

**Files:**
- Modify: `src/components/layout/panel-tabs.tsx`

- [ ] **Step 1: Replace the tab button styling**

Replace the entire `panel-tabs.tsx` with:

```tsx
import { useState } from "react"

export interface PanelTab {
  id: string
  label: string
  icon?: React.ReactNode
  content: React.ReactNode
}

interface PanelTabsProps {
  tabs: PanelTab[]
  defaultTab?: string
  className?: string
}

export function PanelTabs({ tabs, defaultTab, className }: PanelTabsProps) {
  const [activeTab, setActiveTab] = useState(defaultTab ?? tabs[0]?.id ?? "")
  const active = tabs.find((t) => t.id === activeTab) ?? tabs[0]

  return (
    <div className={`flex min-h-0 min-w-0 flex-col overflow-hidden ${className ?? ""}`}>
      {/* Tab bar */}
      <div className="flex shrink-0 items-center gap-1 overflow-x-auto border-b border-border bg-muted/30 px-2 py-1.5">
        {tabs.map((tab) => (
          <button
            key={tab.id}
            data-active={tab.id === activeTab || undefined}
            className="flex shrink-0 items-center gap-1.5 rounded-full px-3 py-1 text-[11px] font-medium text-muted-foreground transition-colors hover:bg-primary/8 hover:text-primary data-[active]:bg-primary data-[active]:text-primary-foreground"
            onClick={() => setActiveTab(tab.id)}
          >
            {tab.icon}
            {tab.label}
          </button>
        ))}
      </div>

      {/* Content */}
      <div className="min-h-0 min-w-0 flex-1 overflow-hidden">
        {active?.content}
      </div>
    </div>
  )
}
```

Key changes: `rounded-full` pill shape, `bg-primary`/`text-primary-foreground` for active, `gap-1` between pills, `py-1.5` vertical padding on tab bar.

- [ ] **Step 2: Verify and commit**

```bash
bun run typecheck
git add src/components/layout/panel-tabs.tsx
git commit -m "feat: pill-shaped tabs — rounded-full, primary fill on active"
```

---

## Task 3: Panel Header Polish

**Files:**
- Modify: `src/components/ui/panel-header.tsx`

- [ ] **Step 1: Update the panel header**

Replace the className and title span in `panel-header.tsx`:

Change the `className` on the outer div from:
```
"flex min-h-11 items-center justify-between border-b border-border bg-card px-3 shadow-[inset_0_1px_0_0_rgba(255,255,255,0.03)]"
```
to:
```
"flex min-h-10 items-center justify-between border-b border-border bg-card px-3"
```

Change the title span className from:
```
"flex items-center gap-2 text-[0.6875rem] font-medium uppercase tracking-wider text-muted-foreground"
```
to:
```
"flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-muted-foreground"
```

- [ ] **Step 2: Verify and commit**

```bash
bun run typecheck
git add src/components/ui/panel-header.tsx
git commit -m "fix: panel header — larger text, remove dark-only shadow"
```

---

## Task 4: Toolbar Polish

**Files:**
- Modify: `src/components/layout/toolbar.tsx`

- [ ] **Step 1: Fix badge size and warm the toolbar**

In `toolbar.tsx`, change the badge className on line 82 from:
```
className="capitalize text-[0.5rem]"
```
to:
```
className="capitalize text-[0.625rem]"
```

- [ ] **Step 2: Verify and commit**

```bash
bun run typecheck
git add src/components/layout/toolbar.tsx
git commit -m "fix: toolbar badge size — 10px instead of 8px"
```

---

## Task 5: Menu Bar Warmth

**Files:**
- Modify: `src/components/layout/menu-bar.tsx`

- [ ] **Step 1: Update menu bar styling**

In `menu-bar.tsx`, find the outer `div` with `className` containing `bg-card/80 backdrop-blur-sm`. Change it to `bg-muted backdrop-blur-sm`.

Also find the "Manna" label span (the first menu label rendered in the menu loop, or a separate span) and add `text-primary` to make it forest-green.

The exact changes depend on how the menu bar renders its first item — read the file to find the "Manna" text and ensure it has `text-primary font-semibold` styling.

- [ ] **Step 2: Verify and commit**

```bash
bun run typecheck
git add src/components/layout/menu-bar.tsx
git commit -m "feat: warm menu bar — muted background, primary-colored Manna label"
```

---

## Task 6: Detection Cards — Warm Styling

**Files:**
- Modify: `src/components/panels/detections-panel.tsx`

- [ ] **Step 1: Update DetectionCard styling**

In `detections-panel.tsx`, find the DetectionCard component's outer div (around line 63-64):

Change from:
```tsx
<div className="border-b border-border p-3 last:border-0">
```
to:
```tsx
<div className="p-2">
  <div className="rounded-xl border border-border bg-surface-elevated p-3">
```

And add a closing `</div>` before the existing closing `</div>` of the card.

Change the verse text paragraph (around line 74) to use serif font:
```tsx
<p className="mt-1 line-clamp-2 font-serif text-sm leading-relaxed text-muted-foreground">
```

Change the Present button (around line 80) to pill shape:
```tsx
<Button size="sm" className="gap-1 rounded-full" onClick={handlePresent}>
```

Change the Queue button (around line 83-84) to pill shape:
```tsx
<Button variant="outline" size="sm" className="gap-1 rounded-full" onClick={...}>
```

- [ ] **Step 2: Verify and commit**

```bash
bun run typecheck
git add src/components/panels/detections-panel.tsx
git commit -m "feat: warm detection cards — rounded-xl, serif verse text, pill buttons"
```

---

## Task 7: Queue Panel — Active Item Styling

**Files:**
- Modify: `src/components/panels/queue-panel.tsx`

- [ ] **Step 1: Update QueueItemRow active state**

In `queue-panel.tsx`, find the QueueItemRow outer div (around line 51-57). Change the active styling from:
```tsx
isActive
  ? "border border-primary/30 bg-primary/10"
  : "hover:bg-muted/50"
```
to:
```tsx
isActive
  ? "bg-primary text-primary-foreground"
  : "hover:bg-muted/50"
```

Also change the reference text inside QueueItemRow (around line 63) — when active, it should be white:
```tsx
<span className={cn(
  "flex-1 truncate text-sm font-medium",
  isActive ? "text-primary-foreground" : "text-foreground"
)}>
```

Change the badge styling to be more readable — the `text-[0.5rem]` badges on lines 38 and 44 should become `text-[0.625rem]`.

Update the outer QueueItemRow div to use rounded-xl:
```tsx
"group flex h-11 items-center gap-2 rounded-xl px-3 transition-colors"
```

- [ ] **Step 2: Verify and commit**

```bash
bun run typecheck
git add src/components/panels/queue-panel.tsx
git commit -m "feat: warm queue items — green active state, rounded-xl, readable badges"
```

---

## Task 8: Broadcast Monitor Component

**Files:**
- Create: `src/components/broadcast/broadcast-monitor.tsx`

- [ ] **Step 1: Create the broadcast monitor component**

Create `src/components/broadcast/broadcast-monitor.tsx`:

```tsx
import { useBroadcastStore } from "@/stores"
import { CanvasVerse } from "@/components/ui/canvas-verse"

export function BroadcastMonitor() {
  const activeTheme = useBroadcastStore((s) => {
    const id = s.activeThemeId
    return s.themes.find((t) => t.id === id) ?? s.themes[0]
  })
  const liveVerse = useBroadcastStore((s) => s.liveVerse)
  const isLive = useBroadcastStore((s) => s.isLive)

  return (
    <div className="flex flex-col gap-2 border-t border-border bg-[#0d0d0c] p-2">
      {/* Preview — next verse */}
      <div>
        <div className="mb-1 flex items-center justify-between">
          <span className="text-[9px] font-semibold uppercase tracking-widest text-white/35">
            Preview — Next
          </span>
          <button className="rounded bg-destructive/80 px-2 py-0.5 text-[9px] font-bold uppercase tracking-wide text-white transition-colors hover:bg-destructive">
            Take →
          </button>
        </div>
        <div className="flex aspect-video items-center justify-center overflow-hidden rounded-md border border-white/8 bg-gradient-to-b from-[#0a0a0a] to-[#1a1a2e]">
          <p className="px-4 text-center font-serif text-[10px] leading-relaxed text-white/50">
            Select a verse to preview
          </p>
        </div>
      </div>

      {/* Program — live output */}
      <div>
        <div className="mb-1 flex items-center justify-between">
          <div className="flex items-center gap-1.5">
            {isLive && (
              <div className="h-1.5 w-1.5 rounded-full bg-destructive shadow-[0_0_6px_rgba(239,68,68,0.5)]" />
            )}
            <span className={`text-[9px] font-semibold uppercase tracking-widest ${isLive ? "text-destructive" : "text-white/35"}`}>
              Program {isLive ? "— Live" : ""}
            </span>
          </div>
          {isLive && (
            <button className="rounded border border-destructive/25 bg-destructive/12 px-2 py-0.5 text-[9px] font-semibold uppercase text-destructive transition-colors hover:bg-destructive/20">
              Off Air
            </button>
          )}
        </div>
        <div className={`flex aspect-video items-center justify-center overflow-hidden rounded-md bg-gradient-to-b from-[#0a0a0a] to-[#1a1a2e] ${isLive ? "border-2 border-destructive/30" : "border border-white/8"}`}>
          {liveVerse ? (
            <div className="flex flex-col items-center px-4 text-center">
              <p className="font-serif text-[10px] leading-relaxed text-white/85">
                {liveVerse.text}
              </p>
              <p className="mt-1.5 text-[7px] uppercase tracking-[2px] text-white/40">
                {liveVerse.reference} — {liveVerse.translation}
              </p>
            </div>
          ) : (
            <p className="text-[10px] text-white/30">No verse on air</p>
          )}
        </div>
      </div>

      {/* Controls */}
      <div className="flex gap-1">
        <button className="flex-1 rounded border border-white/8 bg-white/4 py-1 text-[9px] text-white/40 transition-colors hover:bg-white/8">
          ◀ Prev
        </button>
        <button className="flex-1 rounded border border-white/8 bg-white/4 py-1 text-[9px] text-white/40 transition-colors hover:bg-white/8">
          Next ▶
        </button>
      </div>
    </div>
  )
}
```

Note: The TAKE and Off Air buttons are wired to visual state only for now — the actual preview→program workflow will be connected in Wave 2 when we build the full preview pipeline. For now, the Program monitor shows the current `liveVerse` from the broadcast store.

- [ ] **Step 2: Verify and commit**

```bash
bun run typecheck
git add src/components/broadcast/broadcast-monitor.tsx
git commit -m "feat: add broadcast monitor — stacked Preview + Program with TAKE/Off Air"
```

---

## Task 9: Workspace Layout Restructure

**Files:**
- Modify: `src/components/layout/workspace.tsx`

- [ ] **Step 1: Update imports**

Add the BroadcastMonitor import:
```tsx
import { BroadcastMonitor } from "@/components/broadcast/broadcast-monitor"
```

Remove the PreviewPanel import:
```tsx
// DELETE: import { PreviewPanel } from "@/components/panels/preview-panel"
```

- [ ] **Step 2: Update center panel tabs**

Remove the Broadcast Preview tab from center panel. Change the center tabs array from:
```tsx
tabs={[
  { id: "detections", label: "Detections", content: <DetectionsPanel /> },
  { id: "broadcast", label: "Broadcast Preview", content: <PreviewPanel /> },
  { id: "analytics", label: "Analytics", content: <Placeholder label="Analytics" /> },
]}
```
to:
```tsx
tabs={[
  { id: "detections", label: "Detections", content: <DetectionsPanel /> },
  { id: "analytics", label: "Analytics", content: <Placeholder label="Analytics" /> },
]}
```

- [ ] **Step 3: Restructure right panel**

Replace the right panel section (from `{/* Right panel */}` to `</Panel>` before `</Group>`) with a vertical split:

```tsx
{/* Right panel — queue + broadcast monitor */}
<Panel id="right" defaultSize="28%" minSize="15%" maxSize="40%">
  <div className="flex h-full flex-col overflow-hidden">
    {/* Queue tabs — top section */}
    <div className="min-h-0 flex-1 overflow-hidden">
      <PanelTabs
        className="h-full"
        defaultTab="queue"
        tabs={[
          { id: "queue", label: "Queue", content: <QueuePanel /> },
          { id: "cross-refs", label: "Cross-refs", content: <Placeholder label="Cross-refs" /> },
          { id: "planner", label: "Planner", content: <Placeholder label="Planner" /> },
        ]}
      />
    </div>
    {/* Broadcast monitor — always visible */}
    <BroadcastMonitor />
  </div>
</Panel>
```

- [ ] **Step 4: Verify and commit**

```bash
bun run typecheck
git add src/components/layout/workspace.tsx src/components/broadcast/broadcast-monitor.tsx
git commit -m "feat: restructure right panel — queue tabs + always-visible broadcast monitor"
```

---

## Task 10: Smoke Test

- [ ] **Step 1: Kill and restart the app**

```bash
pkill -f "target/debug/app" 2>/dev/null
pkill -f "tauri dev" 2>/dev/null
pkill -f "bun run tauri" 2>/dev/null
pkill -f "node.*vite" 2>/dev/null
lsof -ti:3000 | xargs kill -9 2>/dev/null

export PATH="$HOME/.bun/bin:$HOME/.cargo/bin:$PATH"
bun run tauri dev &
disown
```

- [ ] **Step 2: Verify visual changes**

Check:
- Warm cream background (light mode) or warm dark (#161514 range, dark mode)
- Forest-green active pill tabs in all panels
- Rounded detection cards with serif verse text
- Green active queue item
- Broadcast monitor at bottom of right panel (Preview + Program stacked)
- Readable panel headers (12px, not 11px)
- Readable toolbar badge (10px, not 8px)
- Menu bar with muted background and green "Manna" label

- [ ] **Step 3: Test dark mode toggle**

Click the theme toggle in toolbar. Verify dark mode has warm tones, not cold blue-blacks.

- [ ] **Step 4: Test panel resizing**

Drag resize handles. Verify broadcast monitor stays pinned at bottom of right panel and doesn't get squished.

---

## Summary

| Task | Description |
|------|-------------|
| 1 | Warm color tokens (light + dark OKLCH) |
| 2 | Pill-shaped tabs |
| 3 | Panel header polish (bigger text, no shadow) |
| 4 | Toolbar badge size fix |
| 5 | Menu bar warmth |
| 6 | Detection cards — warm styling |
| 7 | Queue panel — active item styling |
| 8 | Broadcast monitor component |
| 9 | Workspace layout restructure |
| 10 | Smoke test |
